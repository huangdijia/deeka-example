{
  "require": {
    "huangdijia/deeka": "dev-master"
  },
  "scripts": {
    "install-dev": [
        "rm -rf .env",
        "echo \"[app]\nstatus=dev\ndebug=true\" > .env",
        "@composer update --dev",
        "@composer dumpautoload"
    ],
    "install-debug": [
        "rm -rf .env",
        "echo \"[app]\nstatus=debug\ndebug=true\" > .env",
        "@composer update --dev",
        "@composer dumpautoload",
        "@composer clearcache"
    ],
    "install-online": [
        "rm -rf .env",
        "@composer update --no-dev",
        "@composer dumpautoload",
        "@composer clearcache"
    ]
  }
}
